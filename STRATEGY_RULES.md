# 전략 규칙

이 프로젝트의 모든 전략은 이 문서를 따른다.
전략 코드는 브랜치별로 분리되지만(`dev1`, `dev2`, ...), **규칙은 `main`에서 하나로 관리**한다.

---

## 1. 매매 원칙

전략을 설계할 때 이 4개는 협상 대상이 아니다. 나머지 규칙은 전부 이걸 코드로 강제하기 위한 장치다.

### 1.1 욕심부리지 않는다

> 낮을 때 사고, 좀 오르면 판다.

- **목표 수익률은 진입 전에 정한다.** 진입 시점에 익절가가 없는 전략은 만들지 않는다.
- **목표 도달 = 청산.** "더 갈 것 같다"는 이유의 홀딩은 없다. 더 먹고 싶으면 트레일링을 *사전에* 규칙으로 넣고 백테스트로 검증한 뒤에 쓴다. 실행 중 판단으로 바꾸지 않는다.
- **진입은 눌린 자리에서.** 급등 추격 진입은 기본적으로 하지 않는다. 추세추종 전략을 만들 경우 그건 예외로 명시하고 별도 검증한다.
- **대상은 살아있는 티커.** 상장폐지·거래정지 위험군은 제외한다. 최소 조건: 24h 거래대금 하한을 전략 상수로 두고 진입 전 확인.

### 1.2 시드를 나눈다

> 계란을 한 바구니에 담지 않는다.

- 전재산 한 방 진입은 없다. **분할은 두 축으로 한다: 티커를 나누고, 시간을 나눈다(분할매수).**
- 구체적 한도는 [2. 자본 배분](#2-자본-배분)에 숫자로 박아둔다.
- **격리 마진(isolated) 필수.** 한 포지션의 사고가 다른 포지션으로 번지지 않게 한다.

### 1.3 완벽한 전략은 없다 — 포트폴리오로 본다

> 어떤 전략이든 벌 때가 있고 잃을 때가 있다. 어떤 티커엔 통하고 어떤 티커엔 안 통한다.

- 그러니 **개별 전략에 전 구간·전 티커 수익을 요구하지 않는다.** 그런 걸 요구하면 결국 백테스트 숫자에 맞춰 파라미터를 깎게 되고, 그건 검증이 아니다.
- 요구하는 건 **합산 기대값이 플러스인가**다. 판정 기준은 [5. 통과 기준](#5-통과-기준).
- 따라서 전략은 **언제든 교체·폐기 가능한 부품**으로 만든다. 특정 전략에 애착을 갖지 않는다. 폐기 기준은 [7. 운영](#7-운영-중-규칙)에 미리 정해둔다.
- 여러 전략을 동시에 돌리는 것이 기본 형태다. 하나가 부진할 때 다른 하나가 버텨주는 구조를 노린다.

### 1.4 AI의 역할

AI가 하는 일과 하면 안 되는 일을 구분한다.

**한다**
- 전략 후보 초안 작성, 백테스트 코드 작성/자동화
- 여러 심볼·기간에 대한 일괄 검증과 결과 리포트
- 전략 간 상관관계 분석, 자본 배분안 제시
- 백테스트-실전 코드 괴리, 정밀도/최소주문 등 실행 버그 점검

**안 한다**
- **백테스트 숫자가 좋아질 때까지 파라미터를 돌리는 것.** 파라미터 탐색은 [4.5 과최적화 방지](#45-과최적화-방지) 절차 안에서만.
- 검증 안 된 전략의 실전 배포
- 규칙에 없는 임의 판단으로 진입/청산

---

## 2. 자본 배분

전체 시드를 `S`라고 할 때:

| 항목 | 한도 | 이유 |
|---|---|---|
| 전략 1개당 최대 배분 | **S의 30%** | 전략 하나가 무너져도 시드의 30%만 노출 |
| 티커 1개당 최대 노출(증거금) | **S의 10%** | 개별 종목 리스크 차단 |
| 1회 진입 크기 | **해당 전략 배분액의 1/3 이하** | 분할매수·추매 여력 확보 |
| 동시 보유 포지션 | **최대 7개** | 관리 가능 범위 |
| 현금(미투입) 비중 | **항상 S의 20% 이상** | 추매 여력과 기회 비용 |

- **1회 진입 1/3 규칙이 핵심이다.** 추매를 전제로 하는 전략에서 첫 진입에 다 넣으면 그 전략은 이미 실패다.
- 레버리지는 **2배 또는 3배**. 그 이상은 쓰지 않는다. 선택 기준은 [부록 A](#부록-a-레버리지와-청산-거리).

---

## 3. 전략 작성 규칙

코드 컨벤션(async, 명명법, 로깅, 금지사항)은 [shrimp-rules.md](shrimp-rules.md)를 따른다. 여기는 **전략 고유 규칙**이다.

### 3.1 백테스트와 실전은 같은 코드를 쓴다

지금까지 가장 크게 데인 부분이다. 백테스트에서 검증한 파라미터와 실전 파라미터가 서로 다르면 그 검증은 존재하지 않는 것과 같다.

- **상수는 한 곳에만 정의한다.** 전략 파일 상단에 두고, 백테스트가 그것을 `import` 한다. 복사 금지.
- **신호 판단은 순수 함수로 뽑는다.** 백테스트와 실전이 문자 그대로 같은 함수를 호출하게 만든다.

```python
# strategies/<name>.py
TIMEFRAME = "15m"
LEVERAGE = 2
TAKE_PROFIT_ROI = 1.00      # ROI +100%

def should_enter(df) -> str | None:   # "long" | "short" | None — 순수 함수
    ...

class XxxStrategy:                    # 실전 실행부 (주문/포지션 관리)
    ...
```

```python
# backtest/<name>.py
from strategies.xxx import TIMEFRAME, LEVERAGE, TAKE_PROFIT_ROI, should_enter   # 재정의 금지
```

- 전략 파일 주석에 "백테스트와 동일"이라고 쓰지 않는다. **import로 증명한다.**

### 3.2 마감된 캔들만 쓴다

- `fetch_ohlcv` 결과의 **마지막 봉은 형성 중**이다. 신호 판단에 쓰지 않는다. `df.iloc[-2]`가 마지막 확정봉이다.
- 진행 중 봉의 고가/저가로 익절·손절을 판단하면 백테스트와 실전이 반드시 어긋난다.
- 예외: 실시간 청산 감시(트레일링 스탑 등)는 현재가를 쓰되, **백테스트도 동일한 방식으로 모사**해야 한다.

### 3.3 주문은 거래소 값에 맞춘다

```python
m = self.exchange.market(self.symbol)
amount = float(self.exchange.amount_to_precision(self.symbol, raw_amount))
price  = float(self.exchange.price_to_precision(self.symbol, raw_price))
if amount * price < m["limits"]["cost"]["min"]:
    return   # 최소 명목가 미달 → 진입 포기
```

- 수량/가격 정밀도를 직접 계산하지 않는다. `amount_to_precision` / `price_to_precision`을 쓴다.
- 최소 주문금액은 하드코딩하지 않고 `market["limits"]["cost"]["min"]`을 읽는다.

### 3.4 손절은 거래소에 걸어둔다

- 코드 기반 손절만 믿지 않는다. 봇이 죽으면 손절도 죽는다.
- 진입 직후 `STOP_MARKET` + `reduceOnly`를 건다. 심볼이 지원하지 않으면 로그로 남기고 코드 손절로 폴백한다.
- **손절 없는 전략은 만들지 않는다.** 추매로 버티는 전략도 "여기서는 접는다"는 최종 라인이 있어야 한다.

### 3.5 재시작해도 상태가 복구되어야 한다

- 봇 재시작 시 포지션 상태(부분익절 여부, 추매 횟수, 최고가 등)를 **추측하지 않는다.**
- 상태는 파일이나 거래소 주문에 기록해두고 읽어서 복원한다. 추측 복원은 이미 오작동을 냈다.

---

## 4. 백테스트 규칙

### 4.1 비용을 반드시 반영한다

| 항목 | 값 | 비고 |
|---|---|---|
| 수수료 | **편도 0.04%** (테이커) | 지정가 전략이면 0.02%, 단 체결 가정을 보수적으로 |
| 슬리피지 | **편도 0.02%** (메이저) / **0.05%** (알트) | 시장가 진입 기준 |
| 펀딩비 | **8시간마다 명목가의 0.01%** | 며칠 보유하는 전략에는 필수 |

- 비용 0으로 돌린 백테스트는 **결과로 인정하지 않는다.**
- 한 트레이드에 진입·부분익절·청산 3번 체결되면 왕복 비용은 대략 0.12~0.15%다. 회전이 빠른 전략일수록 이게 승패를 가른다.

### 4.2 데이터 요건

| 항목 | 최소 | 권장 |
|---|---|---|
| 기간 | **6개월** | 1년 이상 |
| 심볼 수 | **10종** | 20종, 시총 구간 혼합 |
| 트레이드 수 | **100건** | 300건 이상 |

- 500봉(15분봉 기준 약 5일)짜리 결과로 판단하지 않는다.
- `fetch_ohlcv`는 1회 최대 1000봉이므로 **`since` 기반 페이징으로 수집하고 CSV로 캐시**한다. 매번 새로 받지 않는다.
- **상승·하락·횡보 구간이 모두 포함**되어야 한다. 한 국면만 담긴 데이터의 결과는 그 국면의 결과일 뿐이다.
- 단일 심볼 결과로 다른 심볼에 배포하지 않는다.

### 4.3 베이스라인과 비교한다

- `backtest_random_entry.py`(무작위 진입, 동일한 청산 규칙)를 **공식 베이스라인**으로 삼는다.
- 같은 데이터·같은 비용 가정으로 돌려서 **베이스라인을 유의미하게 이겨야** 전략으로 인정한다.
- 이기지 못하면 그 전략의 알파는 진입 신호가 아니라 청산 규칙에 있는 것이다. 그럼 진입 로직을 버리고 청산 규칙만 챙긴다.

### 4.4 결과는 기록으로 남긴다

- 백테스트 결과는 브랜치에 `docs/backtest_<전략명>.md`로 커밋한다.
- 필수 기재: 기간, 심볼 목록, 비용 가정, 트레이드 수, 총수익, MDD, 승률, 손익비, Profit Factor, **심볼별 손익**, 베이스라인 대비.
- 실패한 전략의 리포트도 지우지 않는다. 같은 실패를 반복하지 않기 위한 자산이다.

### 4.5 과최적화 방지

백테스트에서 잘 나오는 파라미터를 찾는 것과, 그게 다음 달에도 통하는 것은 다른 문제다. 그래서 절차를 정한다.

1. 데이터를 **앞 70%(개발용) / 뒤 30%(검증용)**로 나눈다.
2. 파라미터 탐색·수정은 **앞 70%에서만** 한다. 뒤 30%는 보지 않는다.
3. 확정한 뒤 뒤 30%에서 **딱 한 번** 돌린다.
4. 뒤 30%에서 무너지면 **그 전략은 폐기한다.** 파라미터를 다시 만지지 않는다. 다시 만지는 순간 검증용 데이터가 오염되고 그 전략은 영원히 검증 불가가 된다.
- 파라미터 개수는 적을수록 좋다. 최적화 대상 파라미터가 5개를 넘으면 설계를 다시 본다.

---

## 5. 통과 기준

전략 하나가 실전에 나가려면 아래를 **전부** 만족해야 한다. (비용 반영 후 기준)

| 항목 | 기준 |
|---|---|
| 총 손익 | **> 0** |
| MDD (최대 낙폭) | **≤ 30%** |
| Profit Factor | **≥ 1.3** |
| 수익 심볼 비율 | 검증 심볼의 **절반 이상** |
| 베이스라인 | random entry **초과** |
| 최악 구간 손실 | 어떤 3개월 구간에서도 **-25% 이내** |
| 트레이드 수 | **100건 이상** (미달 시 판단 보류, 데이터 추가) |

- 승률은 단독 기준으로 쓰지 않는다. "좀 오르면 판다"는 전략은 **승률이 높고 손익비가 낮은 게 정상**이고, 그 반대도 정상이다. 보는 건 기대값(Profit Factor)이다.
- **"전 심볼 수익"을 요구하지 않는다** ([1.3](#13-완벽한-전략은-없다--포트폴리오로-본다)). 절반 이상이면 충분하다.

---

## 6. 배포 관문

통과 기준을 넘겨도 바로 본 시드를 넣지 않는다.

| 단계 | 기간 | 자본 | 통과 조건 |
|---|---|---|---|
| 1. 테스트넷 | **1주 이상** | - | 에러 없이 진입·청산 전 사이클 최소 5회 완주 |
| 2. 소액 실전 | **2주 이상** | 시드의 **5~10%** | 실전 체결가·슬리피지가 백테스트 가정과 크게 어긋나지 않음 |
| 3. 본 배포 | - | [2. 자본 배분](#2-자본-배분) 한도 내 | - |

- 테스트넷 단계에서 보는 건 수익이 아니라 **버그다.** 정밀도 오류, 최소 주문금액 미달, 상태 복구 실패, 손절 미체결.
- 소액 실전 단계에서 보는 건 **백테스트와 실전의 괴리다.** 진입가 차이, 체결 지연, 예상보다 큰 슬리피지.
- 어떤 단계든 **프로덕션 직접 배포는 금지**한다.

---

## 7. 운영 중 규칙

### 자동 정지 (하드 스톱)

아래에 걸리면 해당 전략을 **자동으로 멈추고 알린다.** 사람 판단을 기다리지 않는다.

- 전략 배분액 대비 **-20%** 도달
- **연속 손절 5회**
- 실전 MDD가 백테스트 MDD의 **1.5배** 초과
- 예외 발생이 10분 내 5회 이상 (거래소/네트워크 이상 가능성)

### 정기 점검 (월 1회)

- 전략별 실전 성과를 기록하고 자본 배분을 조정한다.
- **폐기 기준**: 실전 3개월 연속 손실, 또는 실전 성과가 백테스트 기대치의 절반 이하로 2개월 지속.
- 폐기는 실패가 아니라 정상 절차다 ([1.3](#13-완벽한-전략은-없다--포트폴리오로-본다)).

### 전략 믹스

- 같은 방향·같은 신호를 보는 전략을 중복 편입하지 않는다. (예: EMA 추세추종 2개) 분산 효과가 없으면서 노출만 2배가 된다.
- 편입 전 **기존 전략들과의 손익 상관계수를 확인**한다. 0.7 이상이면 둘 중 하나만 쓰거나 배분을 합쳐서 잡는다.
- 서로 다른 국면에서 버는 전략을 섞는 게 목표다. (예: 되돌림 전략 + 추세 전략)

---

## 8. 브랜치 워크플로

```
main (규칙 + 공용 골격 + 기본 전략)
 └─ dev1  전략 개발 → 백테스트 → 리포트 → 기준 통과 → 테스트넷 → 소액 → main 병합
 └─ dev2  ...
```

1. `main`에서 분기해 `devN` 생성
2. `strategies/<name>.py` (상수 + 순수 신호 함수 + 실행 클래스) 작성
3. `backtest/<name>.py` 작성 — **전략 파일에서 상수·함수를 import** ([3.1](#31-백테스트와-실전은-같은-코드를-쓴다))
4. [4. 백테스트 규칙](#4-백테스트-규칙) 대로 검증, `docs/backtest_<name>.md` 커밋
5. [5. 통과 기준](#5-통과-기준) 충족 시 [6. 배포 관문](#6-배포-관문) 진행
6. 소액 실전 통과 후 `main` 병합 및 포트폴리오 편입
- 기준 미달 전략은 브랜치에 리포트만 남기고 병합하지 않는다. 브랜치는 지우지 않는다.

---

## 부록 A. 레버리지와 청산 거리

격리 마진 기준, 추가 증거금 없이 방치했을 때의 대략적인 청산 지점이다. (유지증거금률 약 0.5% 가정, 심볼별로 다름)

| 레버리지 | 롱 청산 (가격) | 숏 청산 (가격) | ROI +100% 도달에 필요한 가격 상승 |
|---|---|---|---|
| 1배 | 약 -99% | 약 +99% | +100% |
| **2배** | 약 **-49%** | 약 **+49%** | +50% |
| **3배** | 약 **-33%** | 약 **+33%** | +33% |
| 5배 | 약 -19% | 약 +19% | +20% |

**ROI와 가격 변동의 관계**: `ROI = 가격변동률 × 레버리지`

| ROI | 3배일 때 가격 | 청산까지 남은 여유 | 2배일 때 가격 | 청산까지 남은 여유 |
|---|---|---|---|---|
| -60% | -20% | **13%p** | -30% | **19%p** |
| -70% | -23% | **10%p** | -35% | **14%p** |

**그래서 레버리지는 이렇게 고른다.**

- **추매·버티기를 전제로 하는 전략 → 2배.** 3배에서 ROI -70%까지 버티면 청산까지 남은 가격 여유가 10%p뿐이다. 추매할 공간이 사실상 없다.
- **손절선이 명확하고 빠르게 접는 전략 → 3배.** 손절이 청산보다 훨씬 앞에 있으므로 청산 거리를 넓게 잡을 이유가 적다.
- **추매는 반드시 증거금을 함께 투입한다.** 수량만 늘리면 평단은 내려가도 청산가는 따라 올라온다. 남은 시드에서 증거금을 넣어 청산가를 밀어내야 의미가 있다.

---

## 부록 B. 배포 전 체크리스트

- [ ] 백테스트가 전략 파일의 상수·함수를 `import` 하는가 (복사본 아님)
- [ ] 수수료·슬리피지·펀딩비를 반영했는가
- [ ] 6개월 이상, 10종 이상, 100트레이드 이상인가
- [ ] 상승·하락·횡보 구간이 모두 포함되었는가
- [ ] 뒤 30% 검증용 데이터에서 한 번만 돌렸는가
- [ ] random entry 베이스라인을 이겼는가
- [ ] MDD ≤ 30%, Profit Factor ≥ 1.3, 절반 이상 심볼 수익인가
- [ ] `docs/backtest_<name>.md` 리포트를 커밋했는가
- [ ] 마감된 캔들(`iloc[-2]`)로 판단하는가
- [ ] `amount_to_precision` / `minNotional`을 적용했는가
- [ ] `STOP_MARKET` 손절을 거래소에 거는가
- [ ] 재시작 시 상태가 복구되는가 (추측 아님)
- [ ] 자동 정지 조건이 구현되어 있는가
- [ ] 테스트넷 1주 → 소액 실전 2주를 거쳤는가
